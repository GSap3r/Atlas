DB_HOST=localhost
DB_PORT=5432
DB_NAME=atlas
DB_USER=postgres
DB_PASSWORD=postgres
DB_POOL_MAX=10

# Chave usada para assinar o cookie de sessão (login). Gere uma vez com
# `python -c "import secrets;print(secrets.token_hex(32))"` e fixe aqui —
# se não definir, o servidor gera uma aleatória a cada início e todo mundo
# é deslogado a cada reinício.
SECRET_KEY=5b1d07b91873aa7e15ef3fd46f7aa1c182caf516a89bf46d6c73240791e79135
